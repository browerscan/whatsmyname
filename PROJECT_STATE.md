# What is My Name production state

Canonical: https://whatismyname.org/

Latest (2026-09-24 16:52 UTC): Worker `f6b20dcd-ae3b-4c66-b039-d03d08dcef80` removes the blog and puts the desktop hero on one line. Blog: routes, article data, `CopyLinkButton`, Article/Blog JSON-LD, footer link, sitemap entries (396 → 333 URLs), the `llms.txt`/`llms-full.txt` article sections, `/blog` `.md` twins, the MCP `list_articles`/`get_article` tools and the `seo.blog`/`pages.blog_*` locale keys are gone; `/blog` URLs return the site's 404 page, and the 63 removed URLs were sent to IndexNow (200). Hero (`components/pages/SearchHero.tsx`): from `lg` up the text block uses the full card width (1,118 px instead of 768) and the highlighted title sits on its own line, so the title and description fit on one line in all nine locales at 1280/1440/1920 px (before: title wrapped in en/de/fr/ja, description in 8 locales); mobile and the 672 px search bar unchanged. Built from the `c2570aa7` source plus that delta only (repo commit `3ed1090`, pending model defaults, stays undeployed). 462 unit tests, preview agent-ready 12 / Markdown 6 / UI/CSP 102/102 / hero 225/225, production agent-ready 12 / UI/CSP 101/101 / hero 225/225, ad-slot scroll overflow 0/78 desktop and 0/108 mobile, ads.txt byte match. Evidence: `ops/releases/ads-20260924-hero/public-qa.json`. Rollback target: `c2570aa7`.

Previous (2026-09-24 09:13 UTC): Worker `c2570aa7-3dd5-49d3-b6b5-e2e11eb63926` fixes the loading-ad overflow found in the GEO release (`app/globals.css` only). A lazy Auto ads slot that is still loading (`ins.adsbygoogle` without `data-ad-status`, 0 px wide) holds a 1,200 px iframe for about a second, which widened the desktop page by 517 px (4 of 84 samples while scrolling production). Auto ads inserts its `div.google-auto-placed` wrappers in the content container, in `main`, in the layout root and directly under `body`, so `.google-auto-placed { overflow-x: clip; }` sits on the wrapper: horizontal only, no scroll container, and the loading iframe stays partly in view (683 px desktop / 195 px mobile) so lazy loading still fires. `overflow: clip` on both axes would hide it, and clipping `main`, the layout root or `body` leaves the overflow (`clip-repro.log`). Production scroll probe after the release: 0 of 84 desktop / 0 of 108 mobile overflow samples, all five ad slots still reach a status (one mobile slot `filled`). Preview UI/CSP 102/102, production agent-ready 11, UI/CSP 101/101, ads.txt byte match. Evidence: `ops/releases/ads-20260924-adclip/public-qa.json`. Rollback target: `acd3fda1`.

Previous (2026-09-24 05:37 UTC): Worker `acd3fda1-e7ce-4c3b-b115-28d191859356` adds GEO on top of the panels release. `/llm.txt` serves the `/llms.txt` file (200, `Link: rel="canonical"` to `/llms.txt`). Every page has a Markdown twin at its URL + `.md` (`/index.html.md` for the home page, `/de.md` for a locale root; `lib/agent/markdown-path.ts`), and HTML pages advertise it with `Link: rel="alternate"; type="text/markdown"`; real files such as `/ASSETS_README.md` stay static. The home page has a 7-question FAQ in all nine locales (`content/faq.ts`), rendered as visible text plus matching `FAQPage` JSON-LD and included in the Markdown home page and `llms-full.txt`. IndexNow key `public/4e07bd1ed7bdfc1a44bbc34248dc43e7.txt`; after a release, submit changed pages with `node scripts/indexnow-submit.mjs <urls>` (the nine home URLs were submitted, 202 Accepted). 462 unit tests, preview agent-ready 11 / Markdown 6 / UI/CSP 94/94, production agent-ready 11 / UI/CSP 93/93, ads.txt byte match. Found during QA: an Auto ads slot that is still loading widened the desktop page by 517 px for about 1 s after scrolling; fixed in the next release (`c2570aa7`). Evidence: `ops/releases/ads-20260924-geo/public-qa.json`, `overflow-probe.json`. Rollback target: `c699b90e`.

Previous (2026-09-24 00:51 UTC): Worker `c699b90e-ad15-4ca5-b1a6-9998cf581612` renders the results header, progress and filter panels as white cards in light mode (`bg-card` + `border-border/80`); dark keeps the translucent gradient. Preview UI/CSP 84/84, production 83/83 (first production run hit a 30 s networkidle timeout while ads loaded; rerun passed), agent-ready 7/7, ads.txt byte match. Evidence: `ops/releases/ads-20260923-panels/public-qa.json`. Rollback target: `99de4166`.

Previous (2026-09-23 13:56 UTC): Worker `99de4166-5802-4661-ba1d-74cd0a9b6848` rebalances light-theme colours (`app/globals.css` only). `.glass`/`.glass-strong`/`.tech-grid` used `hsla(var(--token), a)` with space-separated tokens, which is invalid, so the hero and empty-state cards had no background or border in either theme; now `hsl(var(--token) / a)`. Light tokens: primary `221 83% 53%` (white text 5.2:1, was 3.6:1), title/AI gradient ends `199 89% 42%`, neutral slate page wash, stronger border/input, softer slate shadows. Dark tokens unchanged. Production UI/CSP 78/78 (contrast 5.17 light / 5.88 dark, hero surface renders in both themes), agent-ready 7/7, ads.txt byte match. Evidence: `ops/releases/ads-20260923-light/public-qa.json`. Rollback target: `30fd2ace`.

Previous (2026-09-23 12:39 UTC): Worker `30fd2ace-5ebb-4d62-a6a6-f79cb9a11e7d` fixes the AdSense Auto ads side effects. `app/globals.css` hides `ins.adsbygoogle[data-ad-status="unfilled"]` (Google's documented rule; `unfill-optimized` slots stay visible), so an unfilled slot no longer leaves a 1,200 px iframe widening the desktop page. CSP adds `https://fonts.googleapis.com` to style-src and `https://fonts.gstatic.com` to font-src for the Google Sans stylesheets AdSense injects. Preview UI/CSP 69/69 (with simulated unfilled/optimized slots), production UI/CSP 68/68 with real ad slots (zero CSP violations, page width = viewport), agent-ready 7/7, ads.txt byte match. Evidence: `ops/releases/ads-20260923-adsfix/public-qa.json`. Rollback target: `5595ad32`.

Previous (2026-09-23 10:44 UTC): Worker `5595ad32-174e-4430-bdf9-dc7ff5041654` fixes searches stopping halfway. `app/api/search/whatsmyname/route.ts` used `AbortSignal.timeout(60000)`, which also covered the streamed body. The upstream checks ~7 platforms/s (a full 1,487 takes 210–230 s), so every search was cut at 60 s (~730 results) and the page still said "Search complete". A watchdog replaces it: 20 s for the upstream to answer, then 30 s of silence between chunks, no total deadline. The upstream request is aborted when the visitor leaves. Production probes: octocat and torvalds 1,487/1,487 plus the upstream `completed` line (longest gap between lines 4.4 s). A real browser search reached "1,487 / 1,487 (100%)" in 228 s. 429 unit tests, workerd repro, preview browser checks passed. Site-level production UI QA failed 9 checks for a reason outside this release: AdSense Auto ads began placing slots between 09:44 and 10:57 UTC. An unfilled slot keeps a 1,200 px iframe, so the desktop page is 517 px too wide, and CSP `style-src` blocks AdSense's Google Sans stylesheets. Open follow-up (not yet deployed): hide `ins.adsbygoogle[data-ad-status="unfilled"]` as Google documents, and allow fonts.googleapis.com/fonts.gstatic.com. Evidence: `ops/releases/ads-20260923-search/public-qa.json`. The 60 s `AbortSignal.timeout` in `app/api/ai/analyze/route.ts` is unchanged.

Previous (2026-09-23 09:41 UTC): Worker `c83ab4d2-5c50-44c9-acb6-da2a4789b245` ships the UI polish and the AdSense CSP fix. Header logo mark with a single-row brand on mobile; tighter hero and balanced headings; search bar with the icon inside the input, a stacked button and no shortcut hint on mobile; light-theme `--color-muted` changed from mid-grey `215 16% 47%` to a light surface `214 32% 93%`; tinted result-card badges with readable response times; filter chips are `aria-pressed` buttons; tab labels wrap on mobile; the AI button no longer pulses forever. CSP now allows `https://*.adtrafficquality.google` in script/connect/frame-src, because AdSense's ad traffic quality (sodar) call was blocked on every page. Google only documents a nonce-based strict CSP; move to nonces if new AdSense hosts get blocked. Two deploys: `73283e77` (ads-20260923-ui) failed production QA on a 3px tab overflow at 390px mid-search, fixed by `c83ab4d2` (ads-20260923-ui-tabs). Minimal release over the ads-20260923-agent source (14-file delta); backend/model configuration and ads.txt preserved. Typecheck, lint, 425 unit tests, Cloudflare build, preview browser checks (agent-ready 7, ai-markdown 6, UI/CSP 59) and production browser checks (agent-ready 7, UI/CSP 59 with the search streaming live, zero CSP violations) passed. Evidence: `ops/releases/ads-20260923-ui-tabs/public-qa.json`. Rollback target: `7c943aad`.

Previous (2026-09-23): Worker `7c943aad-a5f5-43c0-b57b-ad8dcf803884` makes the site agent-ready (GEO). `robots.txt` now allows AI answer engines and training crawlers with `Content-Signal: search=yes, ai-input=yes, ai-train=yes` (`/api/` still disallowed); `/llms.txt` and `/llms-full.txt`; every page answers `Accept: text/markdown` with Markdown; HTML carries agent `Link` rels; `/.well-known/api-catalog`, `/.well-known/mcp/server-card.json`, `/.well-known/agent-skills/index.json` (+ `username-search/SKILL.md`) and `/.well-known/ai-catalog.json`; a read-only MCP server at `/mcp` (bundled data only, never calls the search upstream); a WebMCP `search_username` tool on the home page. Code lives in `lib/agent/`. Minimal release over the previous production source; backend/model configuration and ads.txt preserved. Typecheck, lint, 425 unit tests, Cloudflare build, 13 preview browser checks and 7 production browser checks passed. Evidence: `ops/releases/ads-20260923-agent/public-qa.json`. Not done by design: OAuth/auth.md (no authenticated API), Web Bot Auth, DNS-AID records (would need Cloudflare DNS + DNSSEC changes).

Previous (2026-09-22): Worker `c2826d68-7003-4a0e-9981-8dee48c5e123` renders AI assistant Markdown as headings, lists, code, dividers and GFM tables. Raw HTML/images are disabled and unsafe links are filtered. Minimal release over the previous production source; backend/model configuration and ads.txt preserved. Typecheck, lint, 14 component tests, Cloudflare build, 6 preview browser checks and 7 production browser checks passed, including one real AI response. Evidence: `ops/releases/ads-20260922-markdown/public-qa.json`.

Previous (2026-09-21 14:48 UTC): Worker `d96ed331-88e1-4880-8262-a85c5b13c216` serves the AdSense submission-preparation fixes plus the AI analyze recovery. `ads.txt` includes both requested Google DIRECT rows: `pub-7152359349184850` and `pub-8231175075553786`. Production `OPENROUTER_MODEL` and code defaults now start with `openrouter/free` and preserve an ordered free-model fallback pool after the old `minimax/minimax-m3:free` returned OpenRouter 404 and an explicit Nvidia candidate showed in-stream provider overload. Full OpenClaw build chain passed. Public `ads.txt`, health, AI SSE, and real browser AI dialog flow passed. Website-side AdSense blocker remains cleared; account/CMP/review state remains owner-operated. Evidence: `ops/releases/ads-20260921-ai-fallback/public-qa.json`; prior 73-item audit: `docs/ADSENSE-AUDIT-20260921.md`.

Cloudflare account: jessgaojia; Worker: whatsmyname. Existing apex and www routes retained.

2026-09-07: published the Ad.Plus ads.txt from commit 3a9ba18 directly through the registered Cloudflare bridge. Public desktop and mobile browser checks passed. Advertising script and UI modifications remain uncommitted and unpublished.

Receipts: `ops/current-state.json`, `ops/deploy-ledger.jsonl`, `ops/releases/ads-20260907/public-qa.json`.

2026-09-21: replaced the Google ads.txt row with `google.com, pub-7152359349184850, DIRECT, f08c47fec0942fa0` and published the isolated ads.txt release through the registered Cloudflare bridge. Worker version `8cba7408-51d3-4115-9a0f-6162b5027d32`. Canonical HTTP exact QA passed: 200 `text/plain`, no redirect, byte-identical body. Receipt: `ops/releases/ads-20260921/public-qa.json`. Other working-tree changes were excluded.

2026-09-08: updated the existing production Worker OPENROUTER_API_KEY from the user-authorized source and OPENROUTER_MODEL to `minimax/minimax-m3:free`. No application code was deployed. Public OpenClaw browser QA passed: AI template POST returned 200 SSE, assistant response completed, and follow-up input was enabled. Receipt: `ops/releases/ai-20260908/public-qa.json`. GitHub Actions secrets remain unchanged and may overwrite the Worker key on a future workflow deployment. Prior uncommitted AI fallback and advertising work remains unpublished.
